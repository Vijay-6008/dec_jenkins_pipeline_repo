pipeline {
    agent any

    environment {
        DOCKER_USER = "manasa"
        AWS_ACCESS_KEY = "26724737639"
    }

    stages {

        stage('Stage 1') {
            
            steps {
                echo "DOCKER_USER: ${env.DOCKER_USER}"
                echo "AWS_ACCESS_KEY: ${env.AWS_ACCESS_KEY}"
               

                sh '''
                    env

                '''
            }
        }

    }
}
